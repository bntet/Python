高级字符串：Python目前提供两种字符串插值方法：
	* 字符串的'％'运算符。
	* string.Template模块。



字符串方法:
内置的字符串类（以及2.6中的unicode类）将获得一个新的方法，'format'，它接受任意数量的位置和关键字参数

格式字符串:
格式字符串由混合字符数据和标记组成。
"My name is {0} :-{{}}".format('Fred')
"My name is Fred :-{}"

简单和复合字段:
简单字段名称可以是名称或数字。如果是数字，它们必须是有效的基数为10的整数; 如果是名称，它们必须是有效的Python标识符。数字用于标识位置参数，而名称用于标识关键字参数。

复合字段名称是表达式中多个简单字段名称的组合：

格式说明符:
每个字段还可以指定一组可选的“格式说明符”，可用于调整该字段的格式。格式说明符遵循字段名称，冒号（'：'）分隔两者
"My name is {0:8}".format('Fred')

operator标准运算符作为函数函数属于执行对象比较，逻辑运算，数学运算和序列运算的类别。

元祖赋值之谜:

在Python的中，变量赋值采用对象引用的方式，传递的是一个对象的内存地址（像一个指针），在这里一个各项指向了内存中储存了不同数据的实体，对名单实体的修改会成功
